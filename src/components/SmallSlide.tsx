type SmallSlideProps = {
    subData: {
      src: string;
      heading: string;
      posted: string;
    }
  }

export const SmallSlide = ({ subData } : SmallSlideProps) => {
    return (
        <div>
            
        </div>
    );
}